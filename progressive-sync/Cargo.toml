[package]
name = "tytus-progressive-sync"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Progressive shared-folder sync: sync-event-v1 contract types and the exact-key consumer (events → blobs → atomic apply)"
homepage.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }
unicode-normalization = "0.1"

[dev-dependencies]
tempfile = "3"

[lib]
name = "tytus_progressive_sync"
path = "src/lib.rs"
